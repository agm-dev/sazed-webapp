const localStoragePrefix = "sazed";

export const storage = {
  DEFAULT_API_BASE_URL: "http://localhost:4000",
  apiBaseUrlKey: `${localStoragePrefix}-apiBaseUrl`,
  accessTokenKey: `${localStoragePrefix}-accessToken`
};
